# Project: Battleship
This is a project build a website game called **Battleship** in The Oding Project.
In this project, I will review all the lessons I have learned so far and implement all the knowledge points including:
* Markdown file rules: **bold**, *italic*, `code`, [link](www.google.com), ### headings, orderd/unordered list, > quote, -[] checkbox
* basic HTML
* CSS
* building through **npm** and **webpack**
* intermediate Git: branch, remote, changing commit(`git commit --amend`, `git reset`, `git rebase`, `git fetch`, `git merge`, `git remote add upstream`)
* object
* object constructor, factory function, class
* ES6 modules
* TDD(test driven development)
* JSON
* asynchronous code: async/await, promise/then
* seperate application logic from DOM-related staff
* Data Structure
* ...

**You have been this far! Keep going!!!**


